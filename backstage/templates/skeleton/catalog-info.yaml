apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.serviceName }}
  description: ${{ values.description }}
  annotations:
    github.com/project-slug: sky2194/${{ values.serviceName }}
spec:
  type: service
  lifecycle: development
  owner: ${{ values.teamName }}
