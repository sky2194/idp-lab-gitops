apiVersion: v1
kind: Service
metadata:
  name: ${{ values.serviceName }}
  namespace: ${{ values.namespace }}
spec:
  selector:
    app: ${{ values.serviceName }}
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
