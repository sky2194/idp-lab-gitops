apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-ui
  namespace: dev
  labels:
    app: payment-ui
    team: team-angular
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-ui
  template:
    metadata:
      labels:
        app: payment-ui
        team: team-angular
    spec:
      containers:
        - name: payment-ui
          image: sky2108/payment-ui:e9c278b1271633cff1713e9de5119291406fc29a
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
